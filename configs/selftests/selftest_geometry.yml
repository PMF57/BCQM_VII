schema_version: vi_spacetime_config_v0.1
experiment_id: selftest_geometry
description: 'Selftest: selftest_geometry'
variant: full
engine:
  mode: v_glue
  notes: Selftest run
sizes:
- 8
seeds:
- 14
scan:
  n_values:
  - 0.8
steps_total: 800
burn_in_epochs: 150
measure_epochs: 650
W_coh: 100
active_window:
  mode: recency
  hops: 100
glue:
  mapping_mode: linear
  profile: composite_all
  axes:
    shared_bias:
      min: 0.0
      max: 0.0
    phase_lock:
      min: 0.0
      max: 0.0
    domains:
      min: 0.0
      max: 0.0
    cadence_disorder:
      min: 1.0
      max: 1.0
crosslinks:
  allow_junctions: true
  new_event_policy: unique_per_thread
observables:
  beta_junc: 1.5
  tick_definition: v_clock_from_V
  compute_clustering: false
  compute_hub_metrics: false
snapshots:
  enabled: false
output:
  out_dir: outputs/selftest/selftest_geometry
  write_timeseries: false
  timeseries_bins: 50
anomaly_thresholds:
  hubshare_star: 0.9
  max_indegree_factor: 3.0
  degenerate_S_tol: 1.0e-06
space:
  enabled: true
  p_reuse_mode: from_n
  domain_match: true
  allow_cocreate_merge: false
  beta_junc: 1.5
  w_star: 0.3
  log_island_timeseries: false
geometry:
  enabled: true
  walkers: 80
  steps: 800
  fit_t_min: 10
  fit_t_max: 150
